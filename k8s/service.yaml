apiVersion: v1
kind: Service
metadata:
  name: ai-code-review-service
spec:
  selector:
    app: ai-code-review
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
